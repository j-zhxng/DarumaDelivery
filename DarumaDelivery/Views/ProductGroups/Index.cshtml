@model IEnumerable<DarumaDelivery.Models.ProductGroup>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductGroupTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductGroupDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductGroupTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductGroupDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProductGroupID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProductGroupID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProductGroupID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
